{
    "exec-opts": ["native.cgroupdriver=cgroupfs"],
    "log-driver": "json-file",
    "log-opts": {
        "max-size": "100m"
    }{% if docker_run_not_in_container %},
    "storage-driver": "overlay2"
{% endif %}
}
